# Notice

The C# implementation in this directory was reconstructed from this project's own shipped `GuidedArrow.dll` core binary because the original authoring project was unavailable.

It is now an active build target and the intended future production core. Decompilation does not restore original comments, formatting, local-variable names or exact project metadata, so the source-built DLL remains a test candidate until it passes the documented runtime acceptance gate.
